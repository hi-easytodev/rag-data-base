<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Инструкция по созданию RAG и CAG систем в N8N с использованием Supabase

## Введение

Данная инструкция объединяет два подхода к расширению памяти ИИ-агентов в N8N: CAG (Context Augmented Generation) и RAG (Retrieval Augmented Generation). CAG подходит для небольших баз знаний, в то время как RAG необходим для работы с большими объемами данных.[^1][^2]

## Сравнение CAG и RAG подходов

### Context Augmented Generation (CAG)

**Принцип работы:**

- Вся база знаний передается в system prompt
- Используется кэширование промптов для экономии токенов
- Подходит для небольших объемов данных

**Преимущества CAG:**

- Простая реализация — работает "из коробки"
- Быстрые ответы благодаря кэшированию
- Экономия до 75% на токенах при кэшировании[^2]
- Не требует дополнительной инфраструктуры

**Ограничения CAG:**

- Упирается в лимиты контекстного окна модели
- Лимиты провайдера на количество токенов в запросе
- При больших базах может быть дороже RAG
- Оплачивается 75% от полной стоимости токенов даже при кэшировании[^1]


### Retrieval Augmented Generation (RAG)

**Принцип работы:**

- База знаний разбивается на векторы и хранится в векторной БД
- Извлекается только релевантная информация по запросу
- Передается малое количество данных в LLM

**Преимущества RAG:**

- Работа с неограниченными объемами данных
- Поиск по смыслу, а не по ключевым словам
- Снижение вероятности галлюцинаций
- Экономия на токенах при больших базах знаний[^1]

**Когда использовать:**

- База знаний превышает контекстное окно модели
- Постоянное обновление данных
- Критически важна точность поиска по смыслу


## Настройка CAG системы в N8N

### Создание базы знаний в Google Drive

**Шаг 1: Подготовка документа**

```
1. Создайте Google Doc с базой знаний
2. Структурируйте данные для удобства чтения AI
3. Сохраните документ с понятным названием
```

**Шаг 2: Настройка Google Cloud Console**

1. **Переход в консоль:**
    - Зайдите на console.cloud.google.com
    - Создайте новый проект (например, "N8N Food Blogger")
2. **Настройка OAuth Consent Screen:**

```
Application name: Ваше приложение
User type: External (для любых Google аккаунтов)
Developer contact: ваша почта
```

3. **Создание OAuth 2.0 Client:**

```
Application type: Web application
Authorized redirect URIs: (копируется из N8N)
```

4. **Активация необходимых APIs:**
    - Google Drive API
    - Google Docs API

### Интеграция с N8N

**Workflow структура:**

```
[Manual Trigger] → [Google Docs] → [Merge] → [AI Agent]
                      ↓
                 [Edit Fields]
```

**Настройка узлов:**

1. **Google Docs Node:**

```json
{
  "operation": "get",
  "documentId": "ID документа или URL",
  "credential": "Google OAuth2 API"
}
```

2. **Edit Fields Node (опционально):**

```javascript
// Для структурирования данных
return {
  text: $input.item.json.text,
  places: $input.item.json.content // база знаний
};
```

3. **AI Agent Node:**

```
System Prompt:
"Ты AI-фудблогер. Отвечай только на основе данных из {{$json.content}}.
Если информации нет в базе знаний, вежливо сообщи об этом."
```


### Кэширование промптов в OpenAI

**Требования для активации кэша:**

- Минимум 1024 токена в промпте[^2]
- Структура промпта: неизменяемая часть + изменяемый запрос пользователя

**Экономия на токенах:**

```
Модель GPT-4o-mini:
- Обычная стоимость: $0.40 за 1M входящих токенов
- С кэшированием: $0.10 за 1M токенов
- Экономия: 75%[^2]
```

**Время жизни кэша:**

- 5-10 минут при стандартной нагрузке
- До 1 часа при низкой нагрузке[^2]


## Настройка RAG системы в N8N

### Архитектура RAG системы

```
[Google Drive Files] → [Document Loader] → [Text Splitter] → [Embeddings] → [Supabase Vector Store]
                                                                                      ↓
[User Query] → [AI Agent] → [Vector Search Tool] ← [Supabase Vector Store]
```


### Настройка Supabase

**Шаг 1: Создание проекта**

1. Зайдите на supabase.com
2. Создайте организацию и проект
3. Выберите регион (ближайший к пользователям)
4. Сгенерируйте пароль для базы данных

**Шаг 2: Инициализация векторной БД**

1. Перейдите в SQL Editor
2. Выберите Quick Starts → LangChain
3. Выполните скрипт для создания таблицы `documents`

**Структура таблицы:**

```sql
CREATE TABLE documents (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  content TEXT,
  metadata JSONB,
  embedding VECTOR(1536)  -- для OpenAI text-embedding-3-small
);
```

**Шаг 3: Получение credentials**

```
Host: Настройки → API → URL
Service Role Secret: Настройки → API → anon/service_role key
```


### Настройка Document Processing Pipeline

**Google Drive Integration:**

1. **Search Files Node:**

```json
{
  "operation": "search",
  "filter": {
    "folder": "ID папки с документами"
  },
  "limit": 50
}
```

2. **Google Docs Get Document:**

```javascript
// Получение содержимого каждого файла
{
  "operation": "get",
  "documentId": "{{$json.id}}"
}
```

3. **Document Loader:**

```json
{
  "operation": "loadSpecificData",
  "textSplitter": "RecursiveCharacterTextSplitter",
  "chunkSize": 5000,    // для полных рекомендаций
  "chunkOverlap": 0     // без перекрытия для коротких документов
}
```

4. **OpenAI Embeddings:**

```json
{
  "model": "text-embedding-3-small",
  "dimensions": 1536
}
```

5. **Supabase Vector Store:**

```json
{
  "operation": "addDocuments",
  "tableName": "documents",
  "batchSize": 100
}
```


### Автоматизация обновлений данных

**Document Manager для отслеживания изменений:**

1. **Supabase Table: document_manager**

```sql
CREATE TABLE document_manager (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  file_id TEXT UNIQUE,
  file_name TEXT,
  hash TEXT,
  last_updated TIMESTAMP DEFAULT NOW()
);
```

2. **Workflow логика обновлений:**

```javascript
// Проверка существования документа
const existingDoc = await supabase
  .from('document_manager')
  .select('*')
  .eq('file_id', fileId);

if (existingDoc.data.length === 0) {
  // Новый документ - добавляем
  await processNewDocument(fileContent, fileId);
} else {
  // Проверяем изменения по хешу
  const currentHash = calculateHash(fileContent);
  const storedHash = existingDoc.data[^0].hash;
  
  if (currentHash !== storedHash) {
    // Документ изменен - обновляем
    await updateDocument(fileContent, fileId, currentHash);
  }
}
```

3. **Удаление документов:**

```javascript
// При удалении файла из Google Drive
await supabase
  .from('documents')
  .delete()
  .eq('metadata->file_id', fileId);
  
await supabase
  .from('document_manager')
  .delete()
  .eq('file_id', fileId);
```


### Query Processing Pipeline

**AI Agent с Vector Store Tool:**

```json
{
  "tools": [
    {
      "name": "Places Vector Search",
      "description": "Используй этот инструмент для получения рекомендаций из векторной базы данных на основе запроса пользователя",
      "operation": "similaritySearch",
      "limit": 1,  // количество результатов
      "similarityThreshold": 0.7
    }
  ]
}
```

**System Prompt для RAG:**

```
Ты AI-фудблогер. Отвечай только на основе данных из инструмента Places Vector Search.

Если инструмент не нашел релевантной информации, вежливо сообщи: 
"К сожалению, у меня нет информации об этом месте в моей базе рекомендаций."

При ответе используй всю найденную информацию из векторной базы.
```


## Практические рекомендации

### Выбор между CAG и RAG

**Используйте CAG когда:**

- База знаний помещается в контекстное окно модели
- Данные редко обновляются
- Нужна максимальная простота реализации
- Бюджет позволяет оплачивать кэширование

**Переходите на RAG когда:**

- База знаний превышает 100-200 KB текста
- Частые обновления данных
- Нужен поиск по смыслу
- Критична стоимость при масштабировании


### Оптимизация производительности

**Для CAG систем:**

```javascript
// Структура промпта для оптимального кэширования
const systemPrompt = `
СТАТИЧНАЯ ЧАСТЬ ПРОМПТА:
- Роль ассистента
- Инструкции по работе
- База знаний: {{$json.knowledge_base}}

ДИНАМИЧЕСКАЯ ЧАСТЬ:
Запрос пользователя: {{$json.user_query}}
`;
```

**Для RAG систем:**

- Настройте similarity threshold (0.7-0.8 для высокой точности)
- Используйте batch processing для загрузки документов
- Мониторьте размер чанков (1000-5000 токенов оптимально)


### Безопасность и лучшие практики

**Управление доступом:**

1. Используйте Service Role ключи только на серверной стороне
2. Настройте Row Level Security (RLS) в Supabase
3. Регулярно ротируйте API ключи

**Мониторинг:**

```javascript
// Логирование поисковых запросов
const searchLog = {
  query: userQuery,
  results_found: vectorResults.length,
  similarity_scores: vectorResults.map(r => r.similarity),
  timestamp: new Date().toISOString()
};
```

**Обработка ошибок:**

```javascript
try {
  const results = await vectorStore.search(query);
  if (results.length === 0) {
    return "К сожалению, не найдено релевантной информации.";
  }
} catch (error) {
  logger.error('Vector search failed:', error);
  return "Произошла ошибка при поиске. Попробуйте переформулировать запрос.";
}
```


## Стоимостный анализ

### CAG система

```
Пример расчета для 10,000 запросов/месяц:
- Размер базы знаний: 50KB (≈12,500 токенов)
- С кэшированием: 12,500 * 10,000 * $0.10/1M = $12.5/месяц
- Без кэширования: 12,500 * 10,000 * $0.40/1M = $50/месяц
```


### RAG система

```
Пример расчета для 10,000 запросов/месяц:
- Средний размер контекста: 2KB (≈500 токенов)
- Стоимость токенов: 500 * 10,000 * $0.40/1M = $2/месяц
- Supabase: $0-25/месяц (в зависимости от объема)
- Embeddings: единоразово при загрузке
```


## Заключение

RAG и CAG системы в N8N предоставляют гибкие решения для расширения памяти ИИ-агентов. CAG подходит для быстрого прототипирования и небольших баз знаний, в то время как RAG обеспечивает масштабируемость и точность поиска для больших объемов данных.[^1][^2]

### Ключевые преимущества решения

- **🚀 No-code подход**: полная настройка без программирования
- **💰 Экономичность**: оптимизация затрат на токены
- **⚡ Автоматизация**: автоматическое обновление базы знаний из Google Drive
- **🔍 Точность поиска**: семантический поиск по смыслу в RAG
- **📊 Масштабируемость**: от простых FAQ до корпоративных баз знаний


### Рекомендации по внедрению

1. **Начните с CAG** для тестирования концепции
2. **Переходите на RAG** при росте базы знаний
3. **Автоматизируйте обновления** через Google Drive интеграцию
4. **Мониторьте стоимость** и производительность системы
5. **Настройте безопасность** доступа к данным

***

**Дата создания документации:** 12 сентября 2025
**Источники:** "Как сделать RAG в n8n на Supabase для ИИ Агента", "Как создать AI-агента БЕЗ КОДА: добавление базы знаний (CAG) в n8n"[^2][^1]
**Применимость:** N8N, Supabase, Google Drive, OpenAI, RAG/CAG системы

<div style="text-align: center">⁂</div>

[^1]: https://www.youtube.com/watch?v=k4a_G5YFSIs

[^2]: https://www.youtube.com/watch?v=FmNjkiNyPKI

