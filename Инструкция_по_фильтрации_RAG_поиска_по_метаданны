<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Инструкция по фильтрации RAG поиска по метаданным в N8N с использованием Qdrant

## Введение

Фильтрация поиска в RAG системе по метаданным позволяет создавать специализированных агентов для разных типов запросов и значительно повышает точность ответов за счёт ограничения области поиска. В этой инструкции рассматривается реализация умной фильтрации с использованием векторной базы данных Qdrant и контекст инжиниринга в N8N.[^1]

## Создание тестовой базы знаний

### Генерация данных с помощью Google Gemini

**Промпт для генерации тестового датасета:**

```
Составь базу знаний про онлайн-школу Vibeschool. 
Все придумай сам. Мне нужен тестовый датасет для теста RAG-системы.
```

**Структура данных:**

- **Формат**: вопрос-ответ пары
- **Объём**: ~100 записей
- **Категории**: школа (общая информация) и курсы (специфическая информация)


### Организация данных в Google Таблице

| Столбец | Назначение | Пример значения |
| :-- | :-- | :-- |
| **Вопрос** | Пользовательский запрос | "Какой у вашей школы сайт?" |
| **Ответ** | Соответствующий ответ | "vibeschool.ai" |
| **Type** | Категория данных | "school" или "курс" |

**Рекомендации по структуре:**

- Первые записи относятся к категории "school" (общая информация о школе)
- Последующие записи - категория "курс" (информация о курсах)
- По 10-15 записей за раз для каждой категории


## Настройка загрузки данных в Qdrant

### Конфигурация Data Loader в N8N

**Последовательность узлов:**

1. **Google Sheets Node**: чтение 100 строк из таблицы
2. **Edit Fields Node (Set)**: объединение полей и создание метаданных
3. **Qdrant Vector Store Node**: загрузка с эмбеддингами

### Настройка Edit Fields Node

```javascript
// Склеивание вопроса и ответа в одно поле
const combinedText = $('Google Sheets').item.json['Вопрос'] + ' ' + $('Google Sheets').item.json['Ответ'];

// Создание поля type для метаданных
const contentType = $('Google Sheets').item.json['Type'];

return {
  json: {
    text: combinedText,
    type: contentType
  }
};
```


### Конфигурация Qdrant Vector Store

**Параметры загрузки:**

- **Embedding модель**: Cohere Multilingual v3
- **Data Loader**: Default Data Loader
- **Метаданные**:
    - Ключ: `type`
    - Значение: поле из данных (`школа` или `курс`)

**Результат в Qdrant:**

```json
{
  "vector": [0.1, 0.2, ...],
  "payload": {
    "text": "Какой у вашей школы сайт? vibeschool.ai",
    "metadata": {
      "type": "school"
    }
  }
}
```


## Архитектура системы агентов

### Главный агент (Orchestrator)

**Функции:**

- Анализ входящих запросов пользователей
- Определение категории вопроса
- Маршрутизация к соответствующему специализированному агенту

**Конфигурация:**

- **Модель**: GPT-4.1 mini
- **Инструменты**:
    - WebSearch агент
    - Агент поиска по школе
    - Агент поиска по курсам
- **Память**: обычная память для сохранения контекста


### Специализированный агент "Школа"

**Назначение**: поиск информации о школе (график работы, преподаватели, общие вопросы)

**Настройки Vector Store:**

```json
{
  "search_filter": {
    "metadata.type": "school"
  }
}
```

**Описание инструмента:**

```
Агент для поиска информации только о школе Vibeschool. 
Использует векторную базу с фильтрацией по метаданным type=school.
```


### Специализированный агент "Курсы"

**Назначение**: поиск информации о курсах (программы, уровни подготовки, содержание)

**Настройки Vector Store:**

```json
{
  "search_filter": {
    "metadata.type": "курс"
  }
}
```

**Описание инструмента:**

```
Агент для поиска информации только о курсах Vibeschool. 
Использует векторную базу с фильтрацией по метаданным type=курс.
```


## Настройка фильтрации в Qdrant Vector Store

### Параметры поиска с фильтрацией

**Для агента "Школа":**

```json
{
  "collection_name": "vibeschool_knowledge",
  "query": "пользовательский запрос",
  "filter": {
    "must": [
      {
        "key": "metadata.type",
        "match": {
          "value": "school"
        }
      }
    ]
  },
  "limit": 5
}
```

**Для агента "Курсы":**

```json
{
  "collection_name": "vibeschool_knowledge", 
  "query": "пользовательский запрос",
  "filter": {
    "must": [
      {
        "key": "metadata.type",
        "match": {
          "value": "курс"
        }
      }
    ]
  },
  "limit": 5
}
```


### Дополнительные настройки

**Reranking модель:**

- Использование rerank модели для дополнительной фильтрации результатов
- Возврат топ-3 наиболее релевантных чанков после реранкинга

**Пример настройки:**

```json
{
  "use_reranker": true,
  "reranker_model": "cohere-rerank-multilingual-v3.0",
  "top_k_rerank": 3
}
```


## Практические примеры использования

### Тестирование фильтрации

**Запрос о школе:**

```
Пользователь: "Какой у вашей школы сайт?"

Маршрутизация: Главный агент → Агент "Школа"
Фильтрация: metadata.type = "school"
Ответ: "vibeschool.ai"
```

**Запрос о курсах:**

```
Пользователь: "На какой уровень подготовки рассчитаны ваши курсы?"

Маршрутизация: Главный агент → Агент "Курсы"  
Фильтрация: metadata.type = "курс"
Ответ: "От начинающих до продвинутых. Основы и мастер-класс. Глубокое обучение."
```


## Контекст инжиниринг

### Концепция очистки контекста

**Проблема:**

- Модели вызывают множество инструментов
- Контекстное окно загрязняется "мусором"
- Снижается точность ответов из-за шума в контексте

**Решение:**

- Очистка контекстного окна перед передачей в основную модель
- Компрессия найденной информации специализированными агентами
- Передача только релевантной информации наверх по иерархии


### Реализация в N8N

**Архитектура потока данных:**

```
Пользователь → Главный агент → Специализированный агент → Vector Store
                     ↑                      ↓
              Очищенная информация ← Компрессия результатов
```

**Пример обработки запроса:**

1. Главный агент получает вопрос: "На какой уровень подготовки рассчитаны курсы?"
2. Запрос перенаправляется агенту "Курсы"
3. Агент выполняет фильтрованный поиск по метаданным type="курс"
4. Reranker выбирает 3 наиболее релевантных чанка
5. Агент компрессирует информацию: "Найденная информация о курсах..."
6. Главный агент получает очищенный контекст и формирует ответ

### Преимущества контекст инжиниринга

- **Снижение шума**: исключение нерелевантной информации
- **Повышение точности**: фокус на важных данных
- **Экономия токенов**: передача только необходимого контекста
- **Улучшение производительности**: более быстрые и точные ответы


## Расширенные возможности фильтрации

### Многоуровневая иерархия агентов

**Структура:**

```
Главный агент
├── Агент "Школа"
│   ├── Подагент "Преподаватели"  
│   └── Подагент "Расписание"
├── Агент "Курсы"
│   ├── Подагент "Начинающие"
│   └── Подагент "Продвинутые"
└── Агент "Техподдержка"
```

**Метаданные для многоуровневой фильтрации:**

```json
{
  "metadata": {
    "type": "курс",
    "level": "начинающий", 
    "subject": "программирование"
  }
}
```


### Динамический выбор моделей

**Модель-селектор:**

```javascript
// Анализ сложности вопроса
const complexity = await analyzeQuestionComplexity(userQuestion);

if (complexity === "сложный") {
    return "gpt-4";
} else {
    return "gpt-3.5-turbo";
}
```

**Условная маршрутизация:**

```json
{
  "conditions": {
    "if": "{{$json.complexity}} === 'сложный'",
    "then": "gpt-4",
    "else": "gpt-3.5-turbo"
  }
}
```


## Оптимизация производительности и стоимости

### Принцип поэтапной оптимизации

1. **Начальная конфигурация**:
    - Самые быстрые и дешевые модели
    - Базовая фильтрация по метаданным
    - Простая архитектура агентов
2. **Постепенное улучшение**:
    - Добавление более дорогих моделей при необходимости
    - Внедрение reranking моделей
    - Усложнение логики фильтрации
3. **Балансировка**:
    - Мониторинг стоимости запросов
    - Оценка качества ответов
    - Поиск оптимального соотношения цена/качество

### Стратегии экономии

**Модельная оптимизация:**

- Использование более дешевых моделей для простых задач
- Применение дорогих моделей только для сложных запросов
- Кэширование частых результатов

**Архитектурная оптимизация:**

- Минимизация количества вызовов LLM
- Эффективная фильтрация перед обращением к модели
- Компрессия контекста перед передачей


## Настройка безопасности и контроля качества

### Валидация метаданных

**Проверка корректности тегирования:**

```python
def validate_metadata(record):
    required_fields = ['type']
    allowed_types = ['school', 'курс']
    
    if 'type' not in record.get('metadata', {}):
        raise ValueError("Missing required metadata field: type")
    
    if record['metadata']['type'] not in allowed_types:
        raise ValueError(f"Invalid type: {record['metadata']['type']}")
    
    return True
```


### Мониторинг качества ответов

**Метрики качества:**

- Точность попадания в нужную категорию
- Релевантность найденных чанков
- Время отклика системы
- Стоимость обработки запросов

**Логирование:**

```json
{
  "timestamp": "2025-09-12T22:11:00Z",
  "user_query": "Какой у вашей школы сайт?",
  "route": "main_agent -> school_agent",
  "filter_applied": "metadata.type=school",
  "results_found": 3,
  "response_time_ms": 1200,
  "cost_tokens": 150
}
```


## Troubleshooting и решение проблем

### Распространенные проблемы

| Проблема | Причина | Решение |
| :-- | :-- | :-- |
| **Неточная маршрутизация** | Плохое описание агентов | Улучшить промпты описания инструментов |
| **Пустые результаты** | Слишком строгая фильтрация | Проверить корректность метаданных |
| **Медленные ответы** | Большое количество чанков | Настроить reranking и limit |
| **Высокие затраты** | Неоптимальная архитектура | Пересмотреть иерархию агентов |

### Диагностические запросы

**Проверка индекса Qdrant:**

```bash
curl -X GET "http://localhost:6333/collections/vibeschool_knowledge/points" \
  -H "Content-Type: application/json"
```

**Тестирование фильтрации:**

```json
{
  "query": "тестовый запрос",
  "filter": {
    "must": [
      {
        "key": "metadata.type",
        "match": {
          "value": "school"
        }
      }
    ]
  },
  "limit": 5
}
```


## Заключение

Фильтрация RAG поиска по метаданным в N8N с использованием Qdrant позволяет:

### Ключевые преимущества

- **🎯 Специализация агентов**: каждый агент фокусируется на своей области
- **🔍 Точность поиска**: фильтрация исключает нерелевантную информацию
- **⚡ Контекст инжиниринг**: очистка контекстного окна от шума
- **💰 Оптимизация затрат**: эффективное использование LLM ресурсов
- **🎛️ Гибкость настройки**: возможность тонкой настройки под задачи
- **🏗️ Масштабируемость**: легкое добавление новых категорий и агентов


### Рекомендации по внедрению

1. **Начните с простой структуры**: 2-3 категории метаданных
2. **Тестируйте на реальных данных**: используйте представительную выборку
3. **Мониторьте качество**: отслеживайте метрики точности и стоимости
4. **Итеративно улучшайте**: постепенно усложняйте архитектуру
5. **Документируйте изменения**: ведите учет всех модификаций системы

***

**Дата создания документации:** 12 сентября 2025
**Источник:** Видео "Фильтрация RAG поиска по метаданным в N8N | Qdrant + контекст инжиниринг"[^1]
**Применимость:** N8N, Qdrant, RAG системы с метаданными

<div style="text-align: center">⁂</div>

[^1]: https://www.youtube.com/watch?v=3u_FU6DS1Hs

