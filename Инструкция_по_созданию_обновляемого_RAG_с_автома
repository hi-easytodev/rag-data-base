<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# Инструкция по созданию обновляемого RAG с автоматической синхронизацией (N8N + Supabase + Google Drive)

## Введение

Обновляемый RAG — это система, которая автоматически отслеживает изменения файлов на Google Drive и поддерживает векторную базу данных в актуальном состоянии. Система точечно обновляет только измененные данные, исключая дублирование и перезапись всей базы, что критически важно для коммерческих high-load приложений.[^1]

## Концепция метаданных в обновляемом RAG

### Краеугольный принцип работы

**Жизненный цикл обновлений:**[^1]

1. **Событие изменения** → триггер срабатывает на создание/обновление файла
2. **Поиск в БД** → находим записи с соответствующими метаданными
3. **Удаление старых данных** → точечное удаление записей конкретного файла
4. **Добавление новых данных** → загрузка обновленного контента с метаданными

### Структура метаданных

**Обязательные поля для отслеживания:**[^1]

```json
{
  "file_id": "1a2b3c4d5e6f7g8h",           // Уникальный ID файла из Google Drive
  "file_title": "document_name.pdf",        // Название файла
  "mime_type": "application/pdf",           // Тип документа
  "url": "https://drive.google.com/...",    // Прямая ссылка на файл
  "last_modified": "2025-09-15T18:44:00Z"  // Время последнего изменения
}
```


## Настройка триггеров в N8N

### Мониторинг изменений Google Drive

**1. Триггер для создания файлов**[^1]

```javascript
// Google Drive Trigger - File Created
{
  "authentication": "googleDriveOAuth2Api",
  "triggerOn": "fileCreated", 
  "folderId": "your_folder_id",
  "pollTimes": {
    "every": "minute"  // Проверка каждую минуту
  }
}
```

**2. Триггер для обновления файлов**[^1]

```javascript  
// Google Drive Trigger - File Updated
{
  "authentication": "googleDriveOAuth2Api", 
  "triggerOn": "fileUpdated",
  "folderId": "your_folder_id", 
  "pollTimes": {
    "every": "minute"
  }
}
```


### Обработка множественных файлов

**Loop Node для пакетной обработки:**[^1]

```javascript
// Loop Node настройки
{
  "loopOverInput": "item",
  "batchSize": 1  // Обработка по одному файлу
}
```


## Стандартизация данных

### Edit Fields Node конфигурация

**Приведение к общему формату:**[^1]

```javascript
// Edit Fields Node
{
  "operations": {
    "set": [
      {
        "name": "file_id",
        "value": "={{ $json.id }}"
      },
      {
        "name": "mime_type", 
        "value": "={{ $json.mimeType }}"
      },
      {
        "name": "file_title",
        "value": "={{ $json.name }}"
      },
      {
        "name": "url",
        "value": "={{ $json.webViewLink }}"
      }
    ]
  }
}
```


## Настройка Supabase для векторного хранения

### Создание дополнительных векторных таблиц

**Проблема повторного создания:**[^1]
При создании второй и последующих векторных таблиц стандартный SQL-скрипт Supabase выдает ошибку из-за попытки пересоздать уже существующий extension.

**Правильный SQL для новой таблицы:**[^1]

```sql
-- НЕ создаем extension повторно, он уже существует

-- Создаем новую таблицу
CREATE TABLE documents_yt_now (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    content TEXT,
    metadata JSONB,
    embedding VECTOR(1536)
);

-- Создаем уникальную функцию поиска для новой таблицы
CREATE OR REPLACE FUNCTION match_documents_yt_now (
    query_embedding VECTOR(1536),
    match_threshold FLOAT DEFAULT 0.78,
    match_count INT DEFAULT 10
) RETURNS TABLE (
    id UUID,
    content TEXT,
    metadata JSONB,
    similarity FLOAT
)
LANGUAGE plpgsql
AS $$
BEGIN
    RETURN QUERY
    SELECT
        documents_yt_now.id,
        documents_yt_now.content,
        documents_yt_now.metadata,
        1 - (documents_yt_now.embedding <=> query_embedding) AS similarity
    FROM documents_yt_now
    WHERE 1 - (documents_yt_now.embedding <=> query_embedding) > match_threshold
    ORDER BY documents_yt_now.embedding <=> query_embedding
    LIMIT match_count;
END;
$$;
```


### Конфигурация Supabase в N8N

**Поиск существующих записей:**[^1]

```javascript
// Supabase Node - Search records
{
  "operation": "selectRows",
  "table": "documents_yt_now", 
  "filterType": "manual",
  "conditions": {
    "conditions": [
      {
        "keyName": "metadata->file_id",
        "condition": "equals",
        "keyValue": "={{ $json.file_id }}"
      }
    ]
  }
}
```

**Удаление устаревших записей:**[^1]

```javascript
// Supabase Node - Delete old records  
{
  "operation": "deleteRows",
  "table": "documents_yt_now",
  "filterType": "manual", 
  "conditions": {
    "conditions": [
      {
        "keyName": "metadata->file_id",
        "condition": "equals", 
        "keyValue": "={{ $json.file_id }}"
      }
    ]
  }
}
```


## Pipeline обработки документов

### Загрузка и обработка файла

**Google Drive Download:**[^1]

```javascript
// Google Drive Node - Download File
{
  "operation": "downloadFile",
  "fileId": "={{ $json.file_id }}",
  "options": {
    "binaryPropertyName": "data"
  }
}
```

**Document Loader настройки:**[^1]

```javascript
// Document Loader Node
{
  "loader": "textLoader", 
  "binaryPropertyName": "data",
  "metadata": {
    "file_id": "={{ $json.file_id }}",
    "file_title": "={{ $json.file_title }}",
    "source": "google_drive"
  }
}
```

**Text Splitter конфигурация:**[^1]

```javascript
// Text Splitter Node  
{
  "splitterType": "recursiveCharacterTextSplitter",
  "chunkSize": 1000,
  "chunkOverlap": 200
}
```


### Векторизация и сохранение

**OpenAI Embeddings:**[^1]

```javascript
// OpenAI Embeddings Node
{
  "model": "text-embedding-3-small",
  "dimensions": 1536
}
```

**Supabase Vector Store запись:**[^1]

```javascript
// Supabase Vector Store Node
{
  "operation": "insert", 
  "tableName": "documents_yt_now",
  "queryName": "match_documents_yt_now",  // Наша кастомная функция
  "options": {
    "metadata": {
      "file_id": "={{ $json.file_id }}",
      "file_title": "={{ $json.file_title }}"
    }
  }
}
```


## Полный Workflow

### Архитектура обработки

```
[Google Drive Triggers] → [Loop] → [Edit Fields] → [Supabase Search] → [Supabase Delete] → [Google Drive Download] → [Document Loader] → [Text Splitter] → [OpenAI Embeddings] → [Supabase Vector Store]
```


### Логика обновления

**Условная обработка:**[^1]

1. **Триггер срабатывает** → файл создан/обновлен
2. **Поиск в БД** → ищем записи по `metadata->file_id`
3. **Если найдены** → удаляем все записи этого файла
4. **Загружаем файл** → скачиваем актуальную версию
5. **Обрабатываем** → разбивка, векторизация
6. **Сохраняем** → новые записи с обновленными метаданными

## Практическое тестирование

### Тест создания файлов

**Сценарий:** добавление нового файла в Google Drive[^1]

```
1. Создать файл "test_333.txt" с контентом "333"
2. Workflow должен:
   - Обнаружить новый файл
   - Скачать содержимое  
   - Создать векторные записи
   - Добавить метаданные с file_id
```


### Тест обновления файлов

**Сценарий:** изменение существующего файла[^1]

```
1. Изменить содержимое файла "test_111.txt" 
2. Workflow должен:
   - Обнаружить изменение
   - Найти старые записи по file_id
   - Удалить только записи этого файла
   - Загрузить новую версию
   - Создать обновленные записи
   
Результат: общее количество записей не увеличивается
```


### Тест пакетной загрузки

**Сценарий:** добавление множества файлов[^1]

```
1. Добавить 6 файлов одновременно в Google Drive
2. Активировать workflow в автоматическом режиме  
3. Через минуту система должна:
   - Обработать все 6 файлов последовательно
   - Создать соответствующие записи в БД
   - Сохранить метаданные для каждого файла
```


## Мониторинг и отладка

### Проверка метаданных

**Просмотр записей в Supabase:**[^1]

```sql
-- Проверка сохранения метаданных
SELECT 
    content,
    metadata->>'file_id' as file_id,
    metadata->>'file_title' as file_title,
    created_at
FROM documents_yt_now 
ORDER BY created_at DESC;
```


### Отладка триггеров

**Проверка выполнения в N8N:**[^1]

1. **Executions tab** → просмотр истории выполнений
2. **Активность триггеров** → проверка срабатывания каждую минуту
3. **Обработка файлов** → количество loop итераций = количеству измененных файлов
4. **Статус записей** → успешность операций с БД

## Экономические преимущества

### Оптимизация ресурсов

**Традиционный подход vs Обновляемый RAG:**[^1]

```
Полная перезапись:
- Обработка всех файлов при каждом изменении
- Расход токенов на векторизацию всей базы
- Время обработки: O(n) где n = общее количество файлов

Точечное обновление: 
- Обработка только измененных файлов
- Расход токенов только на новые/измененные данные
- Время обработки: O(k) где k = количество измененных файлов
```


### ROI расчет

**Экономия для 1000 файлов:**[^1]

```
Сценарий: изменен 1 файл из 1000

Полная перезапись:
- Векторизация: 1000 файлов × $0.02 = $20.00
- Время обработки: ~30 минут

Точечное обновление:
- Векторизация: 1 файл × $0.02 = $0.02  
- Время обработки: ~30 секунд

Экономия: 99.9% стоимости и времени
```


## Масштабирование и оптимизация

### Настройки для production

**Частота мониторинга:**[^1]

- **Развитие:** каждую минуту для тестирования
- **Production:** каждые 5-15 минут в зависимости от нагрузки
- **Enterprise:** webhook-based триггеры для мгновенной реакции

**Пакетная обработка:**[^1]

```javascript
// Оптимизированная обработка множественных изменений
const batchConfig = {
  "maxBatchSize": 10,        // Максимум файлов в пакете
  "processingDelay": 2000,   // Задержка между пакетами (мс)
  "retryAttempts": 3         // Попытки при ошибках
};
```


## Troubleshooting

### Распространенные проблемы

**1. Дублирование записей**[^1]

```
Причина: метаданные не сохраняются при первой записи
Решение: убедиться, что metadata в Document Loader содержит file_id
```

**2. Функция поиска не найдена**[^1]

```
Причина: не указана кастомная функция в Supabase Vector Store
Решение: добавить queryName с именем созданной функции
```

**3. Триггеры не срабатывают**[^1]

```
Причина: неправильно указан folder ID или нет прав доступа
Решение: проверить permissions и корректность folder ID
```


### Мониторинг производительности

**Ключевые метрики:**[^1]

```javascript
const performanceMetrics = {
  "trigger_delay": "< 60 seconds",      // Время срабатывания триггера
  "processing_time": "< 30 seconds",    // Время обработки одного файла  
  "accuracy_rate": "> 99%",            // Точность обновлений
  "cost_per_update": "< $0.05"         // Стоимость одного обновления
};
```


## Заключение и развитие

Обновляемый RAG представляет собой критически важную технологию для коммерческих AI-систем. Система обеспечивает:[^1]

### Ключевые преимущества

- **💰 Экономическая эффективность**: экономия до 99.9% ресурсов при обновлениях
- **⚡ Высокая скорость**: точечные изменения вместо полной перезаписи
- **🔄 Автоматизация**: полностью автономное отслеживание изменений
- **📊 Масштабируемость**: эффективная работа с тысячами файлов
- **🎯 Точность**: исключение дублирования и потери данных


### Области применения

**✅ Рекомендуется для:**

- Корпоративные базы знаний с частыми обновлениями
- Документооборот с множественными авторами
- Системы с высокими требованиями к актуальности данных
- High-load приложения с ограниченным бюджетом на обработку


### Направления развития

1. **Webhook интеграция** для мгновенной реакции на изменения
2. **Многопользовательские разрешения** с учетом прав доступа
3. **Интеграция с другими облачными хранилищами** (Dropbox, OneDrive)
4. **Интеллектуальные метаданные** с автоматическим извлечением сущностей
5. **Версионирование** с возможностью отката к предыдущим состояниям

Обновляемый RAG решает фундаментальную проблему поддержания актуальности корпоративных AI-систем, делая их пригодными для реального коммерческого использования.[^1]

***

**Дата создания документации:** 15 сентября 2025
**Источник:** "Как Создать Обновляемый RAG с ИИ Пошагово Без Кода (n8n, RAG, Supabase, Google Drive)"[^1]
**Применимость:** Автоматизация обновления RAG систем, корпоративные базы знаний, документооборот

<div style="text-align: center">⁂</div>

[^1]: https://www.youtube.com/watch?v=r6WDatH_ua8

